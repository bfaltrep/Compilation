class fiche{
	monTableau: list of client;
	static id: integer;
	nom: string;
	age: integer;
	
	f(a); //ERREUR qui va continuer

	type test = structure {
		test1 : string;
		test2 : integer;
	}

	/* je commente bcp
	genre bcp bcp */
	//commentaire tralala
	
	procedure fiche( tutu: string,){ //ERREUR
		nom = "";
		age = 0;
	}

	procedure fiche2(, lola : string){ //ERREUR
		nom = "";
		age = 0;
	}

	~fiche(){
		t2: list of integer;
		t2 = [1, 3];
		foreach i in t2{
			afficher(t2[i]);
		}
		t2[1] = 3;
	}

	function getNom(): string{
		return nom;
	}

}